// Keep this in-sync with the $breakpoints SCSS map in _mixins.scss
export enum Breakpoints {
  all = '(min-width: 0px)',
  mobile = '(min-width: 400px)',
  tablet = '(min-width: 700px)',
  desktop = '(min-width: 1200px)'
};
